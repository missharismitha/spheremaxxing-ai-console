/**
 * Re-export — chat fallback is implemented in chatFromMydata.ts (mydata.json via realData only).
 */
export {
  answerFromMydataJson,
  findRealRowsForMessage,
  localProcurementChatFallback,
} from "./chatFromMydata";
